# Taskflowy Design Notes

このドキュメントは、Taskflowyの設計上の合意事項を記録するもの。

## 位置づけ

Taskflowyは、Workflowyを唯一のデータソースとするタスク管理＋メモ書き留め＋リマインダーのPWA。

もとはJotflowy（Workflowy公式APIを使ったノートアプリ）のタスクビュー機能として実装されたが、
以下の理由で別アプリとして分離した。

- Jotflowyは「サーバー側に状態を持たないゼロストレージ設計」を方針としている（APIキーは
  暗号化してブラウザのHTTP-only Cookieに保存するのみで、KVやDBに永続化しない）。
- 一方、通知機能（次フェーズで実装予定）には、Push購読情報・通知済み記録・通知設定などの
  サーバー側状態が必須で、これはゼロストレージ設計と両立しない。
- 機能の関心も異なる（Jotflowyは自由記述のメモ書き留め、Taskflowyはタスクの一覧・完了・
  期日管理）ため、無理に1アプリに同居させず、KVを持つ別アプリとして切り出した。

その後、Jotflowyのメモ書き留め機能を本アプリに吸収した（下記「ビュー統合」）。当初の分離は
Jotflowy側のゼロストレージ設計を守るためのものだったので、方向が逆（JotflowyをTaskflowyに
吸収する）の統合は矛盾しない。統合によりメモ機能はKVを持つアプリの中で動くことになり、
Jotflowyのゼロストレージ方針は放棄した。

## ビュー統合（Jotflowy吸収）

デザインは`design_handoff_taskflowy_views/`のハンドオフ資料が正
（配色Charcoal・下部ビューバー・Daily/登録ノードビュー・composeシート・設定「場所」カード）。
主な設計判断は以下の通り。

- **ビュー構造**: Tasks（既存のタスク管理画面そのまま）/ Daily（ネイティブカレンダーの
  デイリーノート履歴）/ 登録ノード（任意ノードの子を1階層表示）を横に並べ、下部ビューバーで
  切り替える。切り替え操作はバー上のスワイプ・タップに限定し、本文領域の左右スワイプ
  （行の完了・削除）と領域で完全に分離する。
- **場所（Place）の統合**: 「閲覧ビューとして登録したノード」と「書き込み先（旧Destination）」を
  1つの「場所」概念に統合した。場所は表示ON/OFF（`inView`）を持ち、OFFなら書き込み先専用
  （Inbox的な使い方）になる。旧destinations設定はクライアント側で自動移行する。
- **Dailyの書き込み先**: Workflowy API の`parent_id`は`today`/`tomorrow`/`next_week`/
  `YYYY-MM-DD`等のカレンダーキーを受け、Day Nodeは未作成でもオンデマンド作成される
  （API リファレンスに明記）。これにより任意日付のデイリーノートへの書き込みが可能。
  クライアントはタイムゾーンのずれを避けるため、相対キーではなく常にローカル日付
  （`YYYY-MM-DD`）を明示して送る。
- **「書き込み先の日付」と「期限」の分離**: composeシートでは日付を指定するUIを
  それぞれ1か所に限定する（書き込み先の日付はセレクタ内のみ、期限はタスクモードの
  タイトル下のみ）。フッターに「書き込み先の日付と、タスクの期限は別ものです。」を常時表示。
- **行操作の統一**: Workflowy上ではタスクもメモも同じノードなので、全ビューの全行が同じ
  3操作（右スワイプ完了トグル・左スワイプ削除確認・タップ詳細）を持つ。Tasksビュー以外では
  チェックボックスを出さず、タスクであることは本文下のTODO/DONEタグだけで示す。

## データ取得

Workflowyの公式APIには、TODOだけを絞り込んで取得する検索APIが無い。そのため、
`GET /nodes-export`（ツリー全体をフラットな配列で返すエンドポイント。ドキュメント上は
1 req/min のレート制限）を使い、Worker側で `layoutMode: "todo"` かつ未完了のノードを
抽出する。

実測値（1ユーザーの実データ）: 約25,000ノード、レスポンスサイズ約10MB、
サーバー内処理込みで約6.4秒、未完了タスク36件。ノード数が多いユーザーほど
レイテンシが伸びる設計になる点は認識した上で採用している。

クライアント側は取得結果を60秒TTLでキャッシュし、stale-while-revalidateで
まず古いキャッシュを表示してからバックグラウンドで再取得する。手動更新ボタンは
キャッシュの新鮮さを無視して強制的に叩くが、その場合もサーバー側のレート制限
（429）はそのまま透過する。

## 期日の表現

タスクの期日は、Workflowyのノード名に埋め込まれる`<time>`マークアップ
（例: `<time startYear="2026" startMonth="7" startDay="28">...</time>`、時刻ありなら
`startHour`/`startMinute`も付く）を正とする。これはWorkflowyのネイティブカレンダー機能が
使っているマークアップと同じ形式で、Workflowy本体・モバイルアプリ側でも同じ表示になる。

別テーブルや別ノードで期日を管理する方式は取らず、ノード名の中に埋め込むことで
Workflowy側の見え方と完全に一致させている。

## 通知（実装済み）

自前のWeb Push（VAPID）による通知を実装した。設計方針は以下の通り。

- VAPIDベースのWeb Push。ペイロード暗号化はRFC 8291 (aes128gcm)、VAPID署名はRFC 8292
  (ES256 JWT)。実装は`src/vendor/webcrypto-web-push/`に取り込み済み（元は npm パッケージ
  `@block65/webcrypto-web-push` v1.0.2、MITライセンス、
  https://github.com/block65/webcrypto-web-push）。2026-07-31時点で最終リリースが
  2024年12月と休眠状態だった一方、Web Pushプロトコル自体は仕様が固まっていて追従の必要が
  ないため、依存を排除して監査可能にする方針でベンダリングした。取り込みはVAPID JWT署名と
  ペイロード暗号化（`buildPushPayload`）のパスのみに絞り、未使用のJWT検証コードは含めて
  いない。WebCrypto APIベースでNode専用実装に依存しない。VAPID鍵ペア自体の生成は
  ライブラリ側でも未提供のため、WebCryptoのECDSA P-256鍵生成で自前実装している。通知に
  タスク名を表示し、タップでアプリを開く。
- Cron Trigger（5分間隔、`wrangler.toml`の`[triggers]`）でnodes-exportを監視し、
  期日が来たタスクを検出する。
- 時刻付きタスクはその時刻を過ぎたら通知（1タスク=1通知）。日付のみのタスクは朝9時JST
  （KVの設定`morningHour`で変更可能）に、その日が期日のタスクをまとめて1通知する。
- KVには以下を保存する: 暗号化したAPIキー（`auth:apikey`。Cronがブラウザcookieなしで
  Workflowy APIを呼ぶために必要）、Push購読情報（`push:subscriptions`、endpoint単位で
  複数端末分）、通知済み記録（`notification:notified:<key>`、TTL 30日）、通知設定
  （`notification:settings`）。
- 二重通知防止は、KVに保存した「通知済み記録」で行う（同じタスク・同じ期日に対して
  一度通知したら再送しない。due変更時は通知キーが変わるため再通知される）。
- 初回導入時に積み残ったoverdueタスクが一斉通知されるのを防ぐため、期日から24時間
  （日付のみタスクは日付が変わった時点）を過ぎた分は送信せず「通知済み」としてのみ記録する。
- 判定ロジックは`src/api/notify.ts`の`selectDueNotifications`という純粋関数に切り出し、
  ユニットテストで仕様を担保している。

この設計のためにKVバインディングが必要であり、それがJotflowyから分離した主な理由。
